{
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "rimraf": "^5.0.5"
  },
  "scripts": {
    "clean:output": "rimraf dist",
    "start": "npm run clean:output && parcel index.html --port 4000",
    "build": "npm run clean:output && parcel build index.html --no-source-maps"
  },
  "dependencies": {
    "assets": "^3.0.1",
    "three": "^0.164.1"
  }
}
